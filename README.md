## 🚀 Getting Started

This project contains two main applications:

- **/server** — Backend API (Node.js, Express, MongoDB)
- **/client** — Frontend Dashboard (React, Tailwind, DaisyUI)

Follow the steps below to run both applications.

---

## ▶️ Start the Backend (Server)

Navigate to the server directory:

```bash
cd server
npm install
npm run dev
```

## ▶️ Start the UI (client)

Navigate to the client directory:

```bash
cd client
npm install
npm start
```